# Monorepo

This repo contains several independent mini-projects, each in its own subdirectory.

## Structure

```
projects/
  battleformations.dev/   # battleformations.dev project
```

## Per-project context

Each project has its own `CLAUDE.md` with project-specific instructions. When working on a project, refer to that file for context, conventions, and guidelines.
